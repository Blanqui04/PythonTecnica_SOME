# deployment/requirements_build.txt - Dependències específiques per al build
# Aquestes són dependències addicionals només necessàries per construir l'aplicació

# Build tools
cx-Freeze==6.14.9
pyinstaller==5.8.0

# Development tools
setuptools>=65.0.0
wheel>=0.38.0
build>=0.10.0

# Additional packaging tools
auto-py-to-exe>=2.32.0  # GUI per PyInstaller (opcional)
nuitka>=1.5.0           # Compilador alternatiu (opcional)
